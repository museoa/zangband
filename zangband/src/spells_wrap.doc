.\src\spells_wrap.c

[ Python Module : spells ]


detect_traps()
        [ returns bool  ]

detect_doors()
        [ returns bool  ]

detect_stairs()
        [ returns bool  ]

detect_treasure()
        [ returns bool  ]

detect_objects_gold()
        [ returns bool  ]

detect_objects_normal()
        [ returns bool  ]

detect_objects_magic()
        [ returns bool  ]

detect_monsters_normal()
        [ returns bool  ]

detect_monsters_invis()
        [ returns bool  ]

detect_monsters_evil()
        [ returns bool  ]

detect_monsters_xxx(match_flag)
        [ returns bool  ]

detect_monsters_string(char *)
        [ returns bool  ]

detect_monsters_nonliving()
        [ returns bool  ]

detect_all()
        [ returns bool  ]

set_blind(v)
        [ returns bool  ]

set_confused(v)
        [ returns bool  ]

set_poisoned(v)
        [ returns bool  ]

set_afraid(v)
        [ returns bool  ]

set_paralyzed(v)
        [ returns bool  ]

set_image(v)
        [ returns bool  ]

set_fast(v)
        [ returns bool  ]

set_slow(v)
        [ returns bool  ]

set_shield(v)
        [ returns bool  ]

set_blessed(v)
        [ returns bool  ]

set_hero(v)
        [ returns bool  ]

set_shero(v)
        [ returns bool  ]

set_protevil(v)
        [ returns bool  ]

set_invuln(v)
        [ returns bool  ]

set_tim_invis(v)
        [ returns bool  ]

set_tim_infra(v)
        [ returns bool  ]

set_oppose_acid(v)
        [ returns bool  ]

set_oppose_elec(v)
        [ returns bool  ]

set_oppose_fire(v)
        [ returns bool  ]

set_oppose_cold(v)
        [ returns bool  ]

set_oppose_pois(v)
        [ returns bool  ]

set_stun(v)
        [ returns bool  ]

set_cut(v)
        [ returns bool  ]

set_food(v)
        [ returns bool  ]

inc_stat(stat)
        [ returns bool  ]

dec_stat(stat,amount,permanent)
        [ returns bool  ]

res_stat(stat)
        [ returns bool  ]

hp_player(num)
        [ returns bool  ]

do_dec_stat(stat)
        [ returns bool  ]

do_res_stat(stat)
        [ returns bool  ]

do_inc_stat(stat)
        [ returns bool  ]

restore_level()
        [ returns bool  ]

lose_all_info()
        [ returns bool  ]

gain_exp(amount)
        [ returns void  ]

lose_exp(amount)
        [ returns void  ]

do_poly_self()
        [ returns void  ]

